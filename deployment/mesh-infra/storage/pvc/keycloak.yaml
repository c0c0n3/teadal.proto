apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keycloak-pvc
spec:
  accessModes:
    - ReadWriteOnce
  # storageClassName: directpv-min-io
  storageClassName: local-storage
  resources:
    requests:
      storage: 1Gi
